// Este archivo se usará automáticamente con 'ng build'
export const environment = {
    production: true,
    // Aún no tenemos esta URL, pero ya la dejamos lista.
    // Esta será la URL de tu backend en Render.
    apiUrl: 'https://tu-api-en-render.onrender.com/api' 
};